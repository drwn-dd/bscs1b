<!DOCTYPE html>
<html>
  <head>
    <title>Hello, World!</title>
    <link rel="stylesheet" href="styles.css" />
  </head>
  <body>
      <form id= "reg form"> 
      <div class="radio-group">
        <input type="radio" name ="gender" value="female" id="female">
        <label for="female">female</label>
        
        <input type="radio" name ="gender" value="male" id="male">
        <label for="male"></label>male</label>
        
        <input type="radio" name ="gender" value="Other" id="other">
        <label for="other"></label>other</label>
        
      </div>
      
      <div class="form group">
        <label for="country">Country: </label>
        <select id="country">
          <option value = "">select a country</option>
          <option value = "USA">United States of America</option>
          <option value = "Canada">United States of America</option>
          <option value = "UK">United States of America</option>
          <option value = "Australia">United States of America</option>
          <option value = "India">United States of America</option>
          <option value = "Philippines">United States of America</option>
          <option value = "Other">United States of America</option>
        </select>
        
        <script>
          function getFormsValues(){
            let fullName = document.getElmentById("fullName").value;
            let email = document.getElmentById("email").value;
            let password = document.getElmentById("password").value;
            let confirmPassword = document.getElmentById("confirmPassword").value;
            let age = document.getElmentById("age").value;
            
            let gender ="";
            if (document.getElmentById("male").checked) {
              gender="Male";
            } else if { (document.getElmentById("Female").checked) {
              gender="Female";
            } else if { (document.getElmentById("other").checked) {
              gender="Other";
            }
            
            let country = document.getElmentById("country").value;
            let termAccepted = document.getElmentById("terms").value;
             
            return{
              fullName: fullName,
              email: email,
              password: password,
              confirmPassword: confirmPassword,
              age: age,
              gender: gender,
              country: country,
              termAccepted: termAccepted
            };
            }
             function validationForm() {
               let values = getElmentById();
               let errors = [];
               
               if (values.fullName ===""){
                 errors.push("Full name is required");
               }
               
               if (values.emails ===""){
                 errors.push("Email is required");
               } else if (values.email.includes("@") || !values.email.includes(".")){
                 errors.push("Please Enter a valid email address");
               }
               
               if (values.password ===""){
                 errors.push("Password is required");
               } else if (values.password.length<6) {
                 errors.push("Password must be atleast 6 characters");
               }
               
               if (values.Password !== values.confirmPassword){
                 errors.push("Password do match");
               }
               
               if (values.age ==="") {
                 errors.push("Age is required");
               } else if { values.age
                 
               }
               
               
               }
               
              
               
             }
            
            
          
          }
        </script>
      </div>
  </body>
</html>
